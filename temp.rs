use std::*;
use std::collections::HashMap;

fn main(&self)  {
let a = 1;
}
fn run_first(&self)  {
let mut bids = vec![];
for i in 1.. nasks + 1 {
bids.push(bid0 * riskfac.pow(i));
}
let mut asks = vec![];
for i in 1.. nasks + 1 {
asks.push(ask0 * riskfac.pow(i));
}
}